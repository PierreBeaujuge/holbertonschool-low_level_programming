#include <stdlib.h>
#include <time.h>
/**
* main - Entry point
*
* Return: Always 0 (Success)
*/
/* betty style doc for function main goes there */
int main(void)
{
char alphabet;
alphabet = 'a'
while (alphabet <= 'z')
putchar(alphabet);
alphabet++;
return (0);
}
